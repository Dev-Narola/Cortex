/**
 * Placeholder for the generated OpenAPI types.
 *
 * Run `pnpm --filter @cortex/api-client generate` (or
 * `pnpm codegen` at the workspace root) to overwrite this file
 * with the real types from the running backend's `/openapi.json`.
 *
 * Until you run codegen at least once, the types are deliberately
 * loose so the build still succeeds — the application code will
 * only gain type-safety once the contract is materialised.
 */

// This is a placeholder; the real file is generated. The build
// expects *some* export, hence the `unknown`-typed stand-ins.

export type components = Record<string, unknown>;
export type paths = Record<string, unknown>;
export type operations = Record<string, unknown>;
