/**
 * Data-display — barrel for read-only data primitives.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 *
 * F1 Part 2 ships: Avatar, Badge. Future F1 parts add Skeleton
 * variants and the data tables.
 */

export { Avatar, type AvatarProps, type AvatarSize, type AvatarTone } from "./Avatar"
export {
  Badge,
  badgeVariants,
  type BadgeProps,
  type BadgeVariantProps,
} from "./Badge"
