/**
 * Layout — barrel for chrome primitives: separators, spacers,
 * containers, grids.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 *
 * F1 ships `Separator`. The full Layout primitives (Container,
 * Grid, Stack, Cluster) land in later parts of F1.
 */

export { Separator } from "./Separator"
