/**
 * Navigation — barrel for tabs, breadcrumbs, pagination, etc.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 *
 * F1 ships `Tabs`; breadcrumb / pagination / stepper land in
 * later parts of F1.
 */

export { Tabs, TabsContent, TabsList, TabsTrigger } from "./Tabs"
