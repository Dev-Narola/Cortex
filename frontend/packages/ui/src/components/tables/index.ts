/**
 * Tables — barrel for Table, DataTable, Pagination, ColumnFilter.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 *
 * F1 ships the empty category. Concrete components land in later
 * parts of F1 (and are used heavily by F3's Documents screen
 * and F7's Settings tables).
 */

export {}
