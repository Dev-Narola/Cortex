/**
 * Input — barrel.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 */

export { Input, type InputProps } from "./Input"
export { inputVariants, type InputVariantProps } from "./input.variants"
