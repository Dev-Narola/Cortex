/**
 * Forms — barrel for every form primitive.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 */

export { Input, type InputProps } from "./Input"
export { Label } from "./Label"
export {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectLabel,
  SelectTrigger,
  SelectValue,
} from "./Select"
