/**
 * Dialogs — barrel.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 */

export {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogOverlay,
  DialogPortal,
  DialogTitle,
  DialogTrigger,
} from "./Dialog"
