/**
 * Hooks — barrel.
 *
 * Re-exported by `@cortex/ui`; never imported directly by app code.
 *
 * **F1 ships an empty barrel.** Future F1 parts will add
 * hooks like `useControllableState`, `useMediaQuery`, and
 * `useReducedMotion` — the placeholders stay here so the
 * import path is stable when they land.
 */

export {}
